[project]
name = "calculator-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "asteval>=1.0.10",
    "fastapi>=0.141.1",
    "pytest>=9.1.1",
    "uvicorn>=0.52.4",
]

[dependency-groups]
dev = [
    "httpx2>=2.12.0",
]
